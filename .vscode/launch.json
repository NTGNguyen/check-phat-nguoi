{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run UV",
            "type":"node",
            "request": "launch",
            "program": "${workspaceFolder}/",
            "console": "integratedTerminal",
            "preLaunchTask": "Run UV",

        }
    ]
}